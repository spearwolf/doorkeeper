{
    "tokenStore": "redis",
    "destroyAllTokensAtStartup": true,
    "redis": {
        "keyNamespace": "doorkeeper.test",
        "client": {
            "url": "redis://redis-test"
        }
    }
}
